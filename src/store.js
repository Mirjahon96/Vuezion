import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

const state = {};

// // Getters
const getters = {};

// Mutations
const mutations = {};

// // Actions
const actions = {};

export default new Vuex.Store({
  state: state,
  getters: getters,
  mutations: mutations,
  actions: actions
});
